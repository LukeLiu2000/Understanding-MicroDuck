# 机械臂运动参数
amplitude1: 2.0   # 关节1摆动幅度 (弧度)
amplitude2: 1.8   # 关节2摆动幅度 (弧度)
freq1: 0.8        # 关节1摆动频率 (Hz)
freq2: 0.5        # 关节2摆动频率 (Hz)
phase2: 0.5       # 关节2初始相位差

# 控制增益（覆盖模型默认值，便于调试）
kp: 1
kd: 0